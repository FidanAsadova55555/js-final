{
  "name": "swiper-centered-auto",
  "version": "1.0.0",
  "description": "A project template for a Swiper-based centered auto slider.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch src/db/db.json --port 3001",
    "start": "serve",
    "build": "echo This is a static template, there is no bundler or bundling involved!"
  },
  "keywords": [
    "swiper",
    "template"
  ],
  "tags": [
    "swiper"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "swiper": "^11.1.15"
  },
  "devDependencies": {
    "@types/swiper": "^5.4.3",
    "serve": "^14.2.4"
  }
}
